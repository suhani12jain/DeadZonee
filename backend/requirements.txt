# ================================================================
# DeadZero v3.1 — requirements.txt (Python 3.11 recommended)
# Install: pip install -r requirements.txt
# ================================================================

# -- Web Framework -----------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
httpx==0.27.0

# -- MongoDB -----------------------------------------------------
motor==3.4.0
pymongo==4.7.2
dnspython==2.6.1

# -- Scientific Computing ----------------------------------------
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2

# -- Image Processing --------------------------------------------
Pillow==10.2.0          # safer than 10.3.0 for compatibility

# -- AI APIs -----------------------------------------------------
anthropic==0.26.0

# -- Report Generation -------------------------------------------
reportlab==4.1.0
matplotlib==3.8.4

# -- Utilities ---------------------------------------------------
python-dotenv==1.0.1
pydantic==2.7.1
aiofiles==23.2.1
requests==2.31.0

# -- Testing (Dev only) ------------------------------------------
pytest==8.2.0
pytest-cov==5.0.0

# -- Notebook (Dev only) -----------------------------------------
jupyter==1.0.0

# ================================================================
# Notes:
# - Use Python 3.11 (IMPORTANT for stability)
# - ML/YOLO/torch NOT included here (handled separately if needed)
# - Jupyter + pytest are dev-only (safe to remove in production)
# ================================================================